import { test } from "@playwright/test";

test("user can log in with valid credentials", async () => {
  console.log("test");
});

test("user sees an error with invalid credentials", async () => {
  console.log("test");
});
